openai>=1.0
gradio>=5.0
requests>=2.31
pandas>=2.2
numpy>=1.26
pyarrow>=15
joblib>=1.3
pyyaml>=6.0
transformers>=4.45
torch>=2.2
# Pinned: the deployed risk scalers and logistic models were pickled with 1.6.1.
# A different minor version can change StandardScaler internals and shift scores silently.
scikit-learn==1.6.1
